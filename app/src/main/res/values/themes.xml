<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OSRnary" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OSRnary" parent="Base.Theme.OSRnary">
        <item name="android:windowLightStatusBar">true</item>
    </style>
    <style name="Theme.OSRnary.Fade" parent="Theme.OSRnary">
        <item name="android:windowAnimationStyle">@style/FadeAnimation</item>
    </style>

    <style name="FadeAnimation">
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>