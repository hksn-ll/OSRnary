# Default (placeholder) secret values so the project compiles without a real
# local.properties. Override these locally in local.properties (gitignored).
# Get a Gemini API key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=MISSING_API_KEY
